% Philosophy Notes — Minimal template
% Bare output: no headers, tight margins, for excerpts and fragments
\documentclass[$if(fontsize)$$fontsize$$else$11pt$endif$, $if(papersize)$$papersize$$else$letterpaper$endif$]{article}

%% Geometry
\usepackage[margin=0.75in]{geometry}

%% Font
\usepackage{fontspec}
\setmainfont{Linux Libertine O}
\setmonofont{DejaVu Sans Mono}[Scale=0.85]

%% Language
\usepackage{polyglossia}
\setdefaultlanguage{english}

%% No page numbers
\pagestyle{empty}

%% Bibliography
$if(biblatex)$
\usepackage[style=$if(bibstyle)$$bibstyle$$else$authoryear$endif$, backend=biber]{biblatex}
$if(bibliography)$\addbibresource{$bibliography$}$endif$
$endif$

%% Hyperlinks
\usepackage{hyperref}
\hypersetup{colorlinks=true, linkcolor=blue!50!black, citecolor=blue!50!black, urlcolor=blue!50!black}

%% Quotations
\usepackage{csquotes}

%% Shared macros
\usepackage[$if(sty_path)$$sty_path$$else$$endif$]{philosophy}

\begin{document}

$body$

$if(biblatex)$\printbibliography$endif$

\end{document}
