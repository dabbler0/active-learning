% Philosophy Notes — Essay template
% Readable essay style: wider margins, Garamond, 1.5-spaced
\documentclass[$if(fontsize)$$fontsize$$else$12pt$endif$, $if(papersize)$$papersize$$else$letterpaper$endif$]{article}

%% Geometry — wider margins for readability
\usepackage[top=1.25in, bottom=1.25in, left=1.5in, right=1.5in]{geometry}

%% Font
\usepackage{fontspec}
\usepackage{unicode-math}
% EB Garamond if available, otherwise Linux Libertine O as fallback
\IfFontExistsTF{EB Garamond}{
  \setmainfont{EB Garamond}[Numbers=OldStyle]
}{
  \setmainfont{Linux Libertine O}
}
\setmonofont{DejaVu Sans Mono}[Scale=0.82]

%% Spacing
\usepackage{setspace}
\setstretch{1.4}

%% Language
\usepackage{polyglossia}
\setdefaultlanguage{english}

%% Bibliography
$if(biblatex)$
\usepackage[style=$if(bibstyle)$$bibstyle$$else$authoryear$endif$, backend=biber, sorting=nyt]{biblatex}
$if(bibliography)$\addbibresource{$bibliography$}$endif$
$endif$

%% Hyperlinks
\usepackage{hyperref}
\hypersetup{
  colorlinks=true,
  linkcolor=black,
  citecolor=black,
  urlcolor=black!70,
}

%% Quotations
\usepackage{csquotes}

%% Graphics
\usepackage{graphicx}

%% Footnotes
\usepackage[perpage, bottom]{footmisc}

%% Shared macros
\usepackage[$if(sty_path)$$sty_path$$else$$endif$]{philosophy}

%% Simple header
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyfoot[C]{\thepage}
\renewcommand{\headrulewidth}{0pt}

%% Title styling
$if(title)$\title{\large $title$$if(subtitle)$\\\normalsize\textit{$subtitle$}$endif$}$endif$
$if(author)$\author{$author$}$endif$
$if(date)$\date{$date$}$else$\date{}$endif$

\begin{document}

$if(title)$\maketitle$endif$
$if(abstract)$
\begin{abstract}
\noindent $abstract$
\end{abstract}
\vspace{1em}
$endif$

$body$

$if(biblatex)$\printbibliography$endif$

\end{document}
