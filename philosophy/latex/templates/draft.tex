% Philosophy Notes — Draft template
% Double-spaced with line numbers and wide left margin for annotation
\documentclass[$if(fontsize)$$fontsize$$else$12pt$endif$, $if(papersize)$$papersize$$else$letterpaper$endif$]{article}

%% Geometry — extra left margin for handwritten notes
\usepackage[top=1in, bottom=1in, left=2in, right=1in]{geometry}

%% Font
\usepackage{fontspec}
\setmainfont{Linux Libertine O}
\setmonofont{DejaVu Sans Mono}[Scale=0.85]

%% Language
\usepackage{polyglossia}
\setdefaultlanguage{english}

%% Double spacing
\usepackage{setspace}
\doublespacing

%% Line numbers
\usepackage{lineno}
\linenumbers
\renewcommand\linenumberfont{\normalfont\small\sffamily\color{gray}}

%% Color (for line numbers)
\usepackage{xcolor}

%% Bibliography
$if(biblatex)$
\usepackage[style=$if(bibstyle)$$bibstyle$$else$authoryear$endif$, backend=biber, sorting=nyt]{biblatex}
$if(bibliography)$\addbibresource{$bibliography$}$endif$
$endif$

%% Hyperlinks
\usepackage{hyperref}
\hypersetup{colorlinks=true, linkcolor=blue!60!black, citecolor=blue!60!black, urlcolor=blue!60!black}

%% Quotations
\usepackage{csquotes}

%% Footnotes
\usepackage[perpage, bottom]{footmisc}

%% Shared macros
\usepackage[$if(sty_path)$$sty_path$$else$$endif$]{philosophy}

%% Header/footer with draft notice
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small\textit{DRAFT — $if(date)$$date$$else$\today$endif$}}
\fancyhead[R]{\small $author$}
\fancyfoot[C]{\thepage}

$if(title)$\title{$title$ \\\small [Draft]}$endif$
$if(author)$\author{$author$}$endif$
$if(date)$\date{$date$}$else$\date{\today}$endif$

\begin{document}

$if(title)$\maketitle$endif$

$body$

$if(biblatex)$\printbibliography$endif$

\end{document}
